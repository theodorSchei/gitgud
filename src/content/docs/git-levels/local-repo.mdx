---
title: "Lokalt repository / Committed"
sidebar:
  # Set a custom order for the link (lower numbers are displayed higher up)
  order: 4
---

## Fra staging til lokalt repository: Sikre endringene dine

Når du har valgt filene og endringene du vil sikre i staging-nivået, er det på tide å lagre dem i prosjektets historie - dette er der lokalt repository-nivå eller "committed" trer inn. En "commit" er en registrert endring i git-historikken, og betraktes som en fast "versjon" eller øyeblikksfoto av prosjektet ditt på det tidspunktet.

## Hvordan lagre endringer i Lokalt Repository?

For å permanent lagre dine staged endringer i det lokale repositoryet, bruker vi git commit-kommandoen. Denne kommandoen tar dine organiserte endringer i staging og lager en commit i git-historikken. Nå er arbeidet ditt trygt lagret og kan gjenopprettes, sammenlignes, eller deles som en del av prosjektets historie.

## Et Praktisk Eksempel

La oss si at du jobber på en programmeringsoppgave i Python. Du har fullført den første oppgaven og er klar til å fange denne milepælen. Du legger `oblig1.py` til i staging og lager en commit med `git commit`. Du gir commiten en beskrivelse som fanger essensen av hva du har oppnådd, og committer den i prosjektets historie. Som et resultat, blir `oblig1.py` en del av git-historikken i ditt lokale repository, klargjort og pakket inn som en ryddig "versjon" av arbeidet ditt så langt.

import SelectCorrect from '../../../components/SelectCorrect.astro';

<SelectCorrect
  question="Hva skjer når du lager en 'commit' i Git?"
  correct="En ny 'versjon' av prosjektet med de staged endringene blir lagret i git-historikken."
  wrong1="Endringene blir umiddelbart sendt til en fjernrepository."
  wrong2="Endringene blir revert til en tidligere versjon av prosjektet."
/>

import LikeThisPage from '../../../components/LikeThisPage.astro';

<LikeThisPage />